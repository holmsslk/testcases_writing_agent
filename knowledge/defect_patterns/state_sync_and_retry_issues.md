# 缺陷模式：状态同步失败与重试逻辑错误

## 状态同步失败
- 设备状态已变更但平台仍显示旧状态。
- 多源上报顺序颠倒，旧版本状态覆盖新版本状态。

## 重试逻辑错误
- 重试未携带原 requestId，导致幂等校验失效。
- 达到最大重试次数后仍持续重发。
- 重试间隔未生效，瞬时高频重发导致链路雪崩。

## 测试关注点
- 状态版本号单调递增校验。
- 重试次数、间隔、停止条件。
- 平台状态修复时限（例如 10 秒）是否满足。

# 缺陷模式：通信超时与回执缺失

## 常见触发条件
- 网络抖动或网关负载升高导致回执延迟。
- 设备执行完成但回执包丢失。
- 平台超时阈值与设备执行时长不匹配。

## 典型表现
- 指令状态长期停留在 PENDING。
- 误判 TIMEOUT 后重复下发，造成设备重复动作风险。
- 告警日志未记录完整的超时上下文（请求ID、重试次数、链路节点）。

## 测试关注点
- 超时阈值边界值（2999ms, 3000ms, 3001ms）。
- 多次重试后状态一致性（最终状态、告警、审计）。
- 超时与成功回执交叉到达时的幂等处理。

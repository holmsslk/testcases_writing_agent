# 领域规则：软硬件联动规则

1. 指令执行必须具备 requestId，设备与平台按 requestId 对齐。
2. 控制指令回执必须包含执行结果、设备时间戳、错误原因（若失败）。
3. 平台状态展示应基于最新有效 stateVersion，不得回退覆盖。
4. 关键控制（分闸、参数重置）必须执行权限校验与审计记录。
5. 设备离线时禁止下发高风险控制指令。
6. 平台与设备时间偏差超过阈值时应触发诊断告警。
